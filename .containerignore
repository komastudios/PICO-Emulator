# The build context only needs scripts/, config/, and the lock files.
# Everything else is excluded.
*

!scripts/
!config/
!manifests/
!revisions.lock
!revisions-debug.lock

# Nothing generated or proprietary, even under the allowed directories.
**/*.img
**/*.zip
**/.DS_Store
**/__pycache__/
.cache/

# Promoted package archives for the import stage (task package / CI).
!dist/picoemulator*.tar.zst
